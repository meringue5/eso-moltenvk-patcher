# Experiment 0007: ESO 12.0.7 target rebase

- Date: 2026-07-20
- Outcome: **succeeded at target rebase and short world rendering; extended stability incomplete**
- Rollback: **not performed; restore path verified before installation**

## Question

Can the verified Experiment 0006 descriptor-compatibility bridge be rebased to
launcher-managed ESO 12.0.7 without changing its 17 redirects, HDR filters, or
MoltenVK controls?

## Hypothesis

If the update changed client content but retained the statically linked
MoltenVK layout, a new exact SHA-256 and UUID should be sufficient. This
hypothesis fails if any wrapper offset, patch byte, external reference, proc
slot, proc query route, embedded-runtime object, or non-game probe changes.

## Update boundary

At approximately 21:02 KST, the user opened the normal Steam launcher for the
prepared Experiment 0006 warm-cache repeat. Before ESO launched, the launcher
installed client 12.0.7, databuild `3281538`. Seven files in the app bundle were
rewritten at 21:02:59, including the executable, active Bink loader, bundled
MoltenVK archive, game manifest, and data payload.

| Property | Superseded build | 12.0.7 build |
|---|---|---|
| ESO SHA-256 | `dcca9fa9012edf7674e048ec3d5123d5e2b4ed6fa2c4e23f04c7ca33f56b4bd3` | `82bc04ebc8c486636303d147edb9af6c0727b19c7faf7ce7d00837ac3e8ebf4d` |
| ESO UUID | `867E93BC-A6E7-3109-BF8E-542FF59CCDFF` | `6599A49F-B1A0-3CBF-9AEF-6D4186A66E0D` |

The launcher UI had reached a Play button when inspected, but also displayed a
server-maintenance notice. No Play or Update control was activated by the agent
and no ESO process was observed. The abandoned prepared evidence directory is
marked invalidated and is not a game-run result.

## Static target evidence

The update did not change the legacy MoltenVK object. Its SHA-256 remains
`7dfdcb34f8e1395410b8bd639c46398a1318429c5a9a6e53784356c86723a85a`,
and the bundled headers still report MoltenVK 1.0.18.

Analysis against the new executable established:

- the MoltenVK link delta remains `0x1a08490`;
- 162 old Vulkan text symbols yield the same 40 external references to exactly
  the same 17 manifest entries;
- MoltenVK 1.4.1 exports every externally referenced entry;
- the prior 17 offsets still identify the same wrappers, and all 17 exact
  12-byte patch preconditions are unchanged;
- the GIPA and GDPA slots remain `0x4b3be90` and `0x4b3be98`;
- static proc recovery finds 19 GIPA sites with 17 unique names and 80 GDPA
  sites with 65 unique names, with no unnamed site;
- comparison against the existing 100-name old/new runtime probes finds no
  missing result and no old-nonnull/new-null regression on ESO's query route.

Generating a target header from
`config/targets-eso-2026-07-20.json` changed only the expected SHA-256 and UUID;
the target count, offsets, names, and patch bytes are identical.

## Preserved installed state

The active loader still begins with the exact 44,400 bytes of the prepared
Experiment 0006 proxy, SHA-256
`fa8b89c2e090c36663f578e6a7d9fae557b0aebc6aa47a03ba4d9faeb3046550`.
The launcher expanded that file to 226,368 bytes with 181,968 zero-filled
trailing bytes, so its whole-file hash no longer identifies the built artifact.
Its embedded target hash is still the superseded SHA-256, and the bridge
constructor will skip patching on the new UUID. The marker and warm cache
remain preserved.

## Completed non-game gate

Process inspection confirmed ESO, Steam, and the launcher were stopped. A
fail-closed cache-preserving restore returned the active loader to the pristine
Bink SHA-256
`c269d54e23a0669037df39a77386f0b5e380f715d4416091d028ab9ca20802eb`
while leaving both pipeline caches at their existing paths and hashes.

Source commit `fa88c6ec44eeadfabfe6b1c8103b37e232711718` fetched and
reverified official MoltenVK 1.4.1, then rebuilt from the original loader and
new target manifest. The build and expanded checks passed:

- Bink symbol re-export and Rosetta self-patch probes;
- controlled default and `descriptor-compat` configuration probes, including
  live resources `1`, argument buffers `0`, MTLHeap `1`, synchronous submits
  `1`, command pooling `1`, and prefill `0` in experiment mode;
- fake and real HDR device-extension filtering from 131 to 130 properties;
- real surface-format baseline of 3 without the HDR pair for legacy 1.0.18,
  and 60 raw to 59 visible with the exact pair removed for 1.4.1;
- legacy and replacement device creation on Apple M4;
- 162 wrapper symbols, 40 external references, 17 covered targets, and no
  externally referenced entry unavailable in 1.4.1;
- 19 GIPA and 80 GDPA direct query sites, no unnamed site, no missing probe
  result, and no old-nonnull/new-null route regression;
- 12 startup-checker tests, two settings-helper tests, Python compilation,
  shell syntax, and whitespace checks.

## Installation checkpoint

Fresh ignored evidence was prepared under
`artifacts/experiment-0007-20260720T121851Z/`. It records the exact source
commit, original/inactive loader, recognized ESO target, SSAO value `0`, and
both unchanged cache fingerprints.

At approximately 21:19 KST, the bridge was installed in `descriptor-compat`
mode while preserving both caches in place. Immediate verification reports:

| Artifact | SHA-256 |
|---|---|
| installed proxy | `08652a3090c1d919dc1f7978027167e5cae30f66932d3289e437c72cb52ea021` |
| renamed original Bink | `f166982931adfef53a23165bc2f73be18016a9a25d1c396dbeb586109f1c9927` |
| MoltenVK 1.4.1 | `d3ee87b2d98c0b7d5db7bcd1e51b010fe998f755f26c09a83768275499b7a398` |
| active warm cache | `971293cca9a9ed748a894aa84aa60dc64bae5c590bb47da9dfed780039252d8c` |
| old cache backup | `72ac0b0dcb4a7bb3bb5b12b150fe923f5814cf38284eb0afe9b12ed6dea07e1c` |

`scripts/status.sh` reports the 12.0.7 target recognized, the installed bridge
target current, and the marker present. No agent launched ESO, Steam, or the
launcher.

## 2026-07-20 maintenance-interrupted run

The user launched through the normal Steam and launcher path at 21:24 KST.
ESO ran for approximately 47 seconds, reached the server-maintenance boundary,
and exited without a crash. The user observed the same transient hot-pink
startup splash seen in the prior bridge runs. Maintenance prevented entry to
character selection or the world, so this run cannot assess the prior
black-layer artifact, general rendering correctness, stuttering, or gameplay
stability.

The isolated bridge run is
`20260720T122412.830609000Z-pid67254`. Its automatic startup verdict passed:

- MoltenVK 1.4.1 loaded with the exact descriptor-compatibility configuration;
- all 17 Vulkan entry points activated against the recognized 12.0.7 target;
- the HDR device-extension view changed from 131 raw to 130 visible entries;
- the HDR surface-format view changed from 60 raw to 59 visible entries;
- device creation succeeded with the three expected legacy extensions and no
  HDR extension;
- no bridge error, Metal compiler warning, or new crash report was recorded;
- Vulkan objects and the instance were destroyed in an orderly sequence.

The installed proxy remains the exact 44,400-byte artifact with SHA-256
`08652a3090c1d919dc1f7978027167e5cae30f66932d3289e437c72cb52ea021`;
the launcher did not append the zero-filled tail observed at the update
boundary. `AMBIENT_OCCLUSION_TYPE` remains `0`, and the complete settings hash
remains
`e71a11c20828ad270c5260b648cd6adb5cd2a7a58be6677acb4a5d5ec3ed49a2`.

The active cache retained its 3,983,422-byte size but changed from SHA-256
`971293cca9a9ed748a894aa84aa60dc64bae5c590bb47da9dfed780039252d8c`
to `f0ff1d02f9167ce71751ebe81d5bcfbb2477710a44f293d533a9bbce14a7e74c`.
The old cache backup remained byte-identical. This proves only that the short
startup rewrote same-sized cache content; it does not establish that shader
compilation caused stuttering or that the cache is now warm for the world.

This is a positive 12.0.7 bridge-startup result and an incomplete gameplay
result, not an experiment failure. The installed configuration is preserved.
The next run requires a fresh evidence boundary after service is available;
the interrupted evidence directory must not be reused as a before-state.

## Next exact user action

No user action is required while the service is under maintenance. After
service returns:

1. Launch through the normal Steam and launcher path.
2. Enter the same character and Auridon area used in Experiment 0006.
3. Make no graphics-setting change and do not enable SSAO, a HUD, or capture.
4. Spend five minutes in the world doing ordinary movement and camera rotation.
5. Stop immediately on a crash, hang, recurring pink/black layer, solid-color
   output, or other severe corruption. Otherwise exit normally after five
   minutes.
6. Report whether rendering remained correct and whether stuttering decreased,
   stayed similar, or increased compared with the first Experiment 0006 run.

The runtime passes only if the automatic bridge verdict passes, no new crash
report or bridge error appears, world rendering remains correct for five
minutes, and the user reports no stop-condition event. An availability stop
before ESO launch is incomplete, not failed.

## 2026-07-21 world-repeat amendment

After service returned, the user launched through the normal Steam path,
entered character selection, loaded Auridon, and explored a limited area. The
user reported correct rendering and no perceptible stuttering. The transient
solid-color startup screen remained. No graphics setting was changed during
the run.

The isolated bridge run is
`20260721T041720.841059000Z-pid76772`. The bridge log and cache timestamps place
teardown approximately 199 seconds after process start. ESO's interface log
records Auridon as fully loaded approximately 38 seconds after start, leaving
an estimated 162-second world interval. It also records the exact pre-UI state
sequence:

- `PlayIntroMovies` from 13:17:24.969 to 13:17:31.454 KST;
- `ShowZOSVideo`, then Havok and legal splash states;
- character selection at 13:17:42.274;
- Auridon systems fully loaded at 13:17:58.075.

Automatic evidence passed:

- MoltenVK 1.4.1 loaded in the exact descriptor-compatibility mode and all 17
  redirects activated;
- the HDR extension and surface-format filters again returned 130 of 131 and
  59 of 60 entries;
- device creation, draw/present proc resolution, and orderly Vulkan teardown
  were observed;
- no bridge error, bridge fatal, or new crash report appeared;
- the executable remained the selected 12.0.7 target before and after the run;
- the settings file remained byte-identical with ambient occlusion `0` and
  `SkipPregameVideos` `0`;
- the active cache retained its 3,983,422-byte size while changing from SHA-256
  `f0ff1d02f9167ce71751ebe81d5bcfbb2477710a44f293d533a9bbce14a7e74c`
  to `fc3f4f31044663a57265837a1b3d64d098dee9b589a195f68441a82eaac7b6eb`.

All 24 ignored evidence files are checksum-verified under
`artifacts/experiment-0007-world-repeat-20260721T041720Z/`. Unified-log capture
was unavailable in the sandbox, so this run has no independent Metal compiler
warning count. The short world interval succeeds for target rebase and
rendering correctness. It does not satisfy the originally planned five-minute
stability interval or constitute a controlled performance A/B.
